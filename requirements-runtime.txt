fastapi
uvicorn[standard]
pydantic>=2
numpy

# HF Hub — model download at container startup
huggingface_hub>=0.23.0

# Your config / utilities if they rely on specific libs
python-dotenv

# Llama backend — pre-built CPU wheel, no source compilation
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
llama-cpp-python
