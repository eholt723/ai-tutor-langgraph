fastapi
uvicorn[standard]
pydantic>=2
numpy

# HF Hub — model download at container startup
huggingface_hub>=0.23.0

# Your config / utilities if they rely on specific libs
python-dotenv

# Llama backend installed separately in Dockerfile with --prefer-binary
